app = 'maxspeed-api'
primary_region = 'sin'

[build]

[env]
  NODE_ENV = 'production'
  PORT = '8080'

[http_service]
  internal_port = 8080
  force_https = true

[[vm]]
  memory = '256mb'
  cpu_kind = 'shared'
  cpus = 1
